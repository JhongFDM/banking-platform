# Playwright QA Suite

This folder is reserved for the new Playwright automation set.

Suggested next steps:
1. Initialize Node project (`npm init -y`).
2. Install Playwright (`npm init playwright@latest`).
3. Keep all Playwright tests and config inside this folder.
